# dir()
# Learn what operations can be performed on an element by using dir():
my_string = 'I am a string'
dir(my_string)
# The elements returned from dir() are all of the methods (i.e. actions) that you can apply to the element.

# type()
type(my_string)

# Use the built-in help system to get full documentation
help(str)

# Import libraries and play with them:
from datetime import datetime
dir(datetime)
datetime.now()